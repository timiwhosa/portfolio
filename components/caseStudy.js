// import Image from 'next/image';
// import Hoverimage from './hoverImage'; // Adjust the import based on your project structure

// const CaseStudyCard = ({ imageSrc, altText, description, previewImage, onClick, hoverImageLink, hoverImageStyle }) => {
//   return (
//     <div
//       onClick={() => onClick(previewImage)}
//       className={`showUiprev relative flex items-end gap-1 py-5 px-10 rounded-3xl cursor-pointer w-max ${hoverImageStyle}`}
//     >
//       <Image
//         src={imageSrc}
//         alt={altText}
//         className="w-auto lg:h-14 md:h-12 sm:h-10 max-[639px]:h-8"
//       />
//       <p className="pb-1 lg:text-2l md:h-8 sm:h-6 max-[639px]:h-3.5">
//         {description}
//       </p>
//       {hoverImageLink && <Hoverimage imageLink={hoverImageLink} />}
//     </div>
//   );
// };

// export default CaseStudyCard;
